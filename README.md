# Night Moon Color Scheme
The `night-moon` contains the new color-scheme. It updates the fpm.color variable (which is of fpm.color-data type). Adding this as the fpm dependency would give a new color scheme.

